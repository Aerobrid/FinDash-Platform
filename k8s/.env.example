# AWS Configuration
AWS_ACCOUNT_ID=YOUR_AWS_ACCOUNT_ID
AWS_REGION=us-east-1

# Database Configuration
DB_HOST=finstream-dev-postgres.XXXXXX.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=finstream

# Note: DB_USERNAME and DB_PASSWORD are stored in K8s Secrets
# Use: kubectl create secret generic finstream-db-secret --from-literal=DB_USERNAME=... --from-literal=DB_PASSWORD=...
